#ifndef UTILS_H
#define UTILS_H

// Generate a random array with values between 1 and max_value
void generate_random_array(int* array, int size, int max_value);

// Swap two integers
void swap(int* a, int* b);

// Print array to console (for debugging)
void print_array(int* array, int size);

#endif